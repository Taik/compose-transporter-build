checkout:
  post:
    - ln -s ~/compose-transporter-build ~/.go_workspace/src/github.com/taik/compose-transporter-build
    - git submodule init
    - git submodule update

dependencies:
  pre:
    - go get github.com/tools/godep
  override:
    - godep go build ./transporter/...
